const assert = require("assert");

describe("RoutesStrategies", function () {
    describe("RoutesStrategies()", function () {
        it("Sample test of RoutesStrategies()", function () {
            assert.equal(1);
        });
    });
});