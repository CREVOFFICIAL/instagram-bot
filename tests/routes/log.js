const assert = require("assert");

describe("RoutesLog", function () {
    describe("RoutesLog()", function () {
        it("Sample test of RoutesLog()", function () {
            assert.equal(1);
        });
    });
});