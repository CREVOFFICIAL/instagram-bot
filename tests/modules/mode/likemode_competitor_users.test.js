const assert = require("assert");

describe("Likemode_competitor_users", function () {
    describe("Likemode_competitor_users()", function () {
        it("Sample test of Likemode_competitor_users()", function () {
            assert.equal(1);
        });
    });
});