const assert = require("assert");

describe("Fdfmode_classic", function () {
    describe("Fdfmode_classic()", function () {
        it("Sample test of Fdfmode_classic()", function () {
            assert.equal(1);
        });
    });
});