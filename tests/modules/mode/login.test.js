const assert = require("assert");

describe("Login", function () {
    describe("Login()", function () {
        it("Sample test of Login()", function () {
            assert.equal(1);
        });
    });
});