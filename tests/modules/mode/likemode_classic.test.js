const assert = require("assert");

describe("Likemode_classic", function () {
    describe("Likemode_classic()", function () {
        it("Sample test of Likemode_classic()", function () {
            assert.equal(1);
        });
    });
});