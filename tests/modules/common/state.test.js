const assert = require("assert");

describe("Manager_state", function () {
    describe("Manager_state()", function () {
        it("Sample test of Manager_state()", function () {
            assert.equal(1);
        });
    });
});