const assert = require("assert");

describe("Slack", function () {
    describe("Slack()", function () {
        it("Sample test of Slack()", function () {
            assert.equal(1);
        });
    });
});