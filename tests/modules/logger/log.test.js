const assert = require("assert");

describe("Log", function () {
    describe("Log()", function () {
        it("Sample test of Log()", function () {
            assert.equal(1);
        });
    });
});